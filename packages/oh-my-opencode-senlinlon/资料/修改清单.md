# 修改清单

## 基于版本
oh-my-opencode v3.1.2

## 修改的文件

### package.json
- `name`: `oh-my-opencode` → `@opencode-ai/oh-my-opencode-senlinlon`
- 添加 `"private": true`
- 删除 `bin` 字段（CLI入口）
- 删除 `optionalDependencies`（平台二进制包）
- 删除 `files` 字段
- 删除 `trustedDependencies`
- 依赖改为 `workspace:*` 和 `catalog:` 格式
- `repository` URL: `code-yeongyu/oh-my-opencode` → `senlinlon/oh-my-opencode-senlinlon`
- `bugs` URL 更新
- `homepage` URL 更新

### 配置文件相关
- 配置文件名: `oh-my-opencode.json` → `oh-my-opencode-senlinlon.json`（所有引用处）
- 缓存目录: `oh-my-opencode` → `oh-my-opencode-senlinlon`
- 日志文件: `oh-my-opencode.log` → `oh-my-opencode-senlinlon.log`

### Schema 文件
- 文件名: `oh-my-opencode.schema.json` → `oh-my-opencode-senlinlon.schema.json`
- URL 改为相对路径 `./oh-my-opencode-senlinlon.schema.json`

### GitHub 引用
- 所有 `code-yeongyu/oh-my-opencode` → `senlinlon/oh-my-opencode-senlinlon`
- 保留了历史 issue 引用（注释中的链接）

### 删除的目录/文件
| 目录/文件 | 原因 |
|-----------|------|
| `src/cli/` | CLI 代码，作为内置插件不需要 |
| `bin/` | CLI 入口脚本 |
| `packages/` | 平台二进制包（darwin, linux, win32） |
| `postinstall.mjs` | 安装后脚本（下载二进制） |
| `bun.lock` | 锁文件（使用 monorepo 的） |
| `node_modules/` | 依赖目录（重新安装） |
| `dist/` | 构建产物（重新构建） |

### 代码修改

#### src/hooks/auto-update-checker/index.ts
- 移除 CLI 依赖导入

## 新增的目录/文件
| 目录/文件 | 用途 |
|-----------|------|
| `资料/` | 维护文档目录 |
| `资料/修改清单.md` | 本文件 |
| `资料/与原版差异.md` | 详细差异说明 |
| `资料/合并指南.md` | 从官方拉取更新的指南 |
| `资料/版本对应.md` | 版本映射关系 |
